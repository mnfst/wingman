---
'wingman': patch
---

The client menu opens on the OpenAI SDK, with the other SDKs behind it, then the agent clients, then Default. Snippet scrollbars are slim and dark instead of the system bar the browser painted across the bottom of any snippet that overflowed, and the highlighted layer no longer slips half a line out of register when a snippet is scrolled to the end.
